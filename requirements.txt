# Core scientific stack
numpy>=2.0
scipy>=1.14
scikit-learn>=1.5
pandas>=2.2
matplotlib>=3.9
seaborn>=0.13

# Deep learning / pretrained models
torch>=2.4
transformers>=4.44

# Notebook environment
jupyter
ipykernel
ipywidgets

# Document conversion (proposal PDFs -> markdown)
markitdown[pdf]

# NOTE: gensim and bnlp-toolkit are deliberately NOT used.
# On Python 3.14 neither ships a wheel and both require a C toolchain.
# Word2Vec (skip-gram + negative sampling) and the Bangla POS tagger
# are therefore implemented from scratch in src/banglastylo/.
